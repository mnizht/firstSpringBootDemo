<!-- definitions 流程文件的根元素
	这个元素中可以定义多个流程（一般建议每个文件中只包含一个流程，这样可以降低开发过程中的维护难度）。
	元素中最少也要包含 xmlns和 targetNamespace的声明。targetNamespace可以是任意值，它用来对流程实例进行分类。
 -->
<definitions id="myProcesses"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://schema.omg.org/spec/BPMN/2.0 BPMN20.xsd"
  xmlns="http://schema.omg.org/spec/BPMN/2.0"
  typeLanguage="http://www.w3.org/2001/XMLSchema"
  expressionLanguage="http://www.w3.org/1999/XPath"
  targetNamespace="http://jbpm.org/example/bpmn2">

<!-- process子元素，每个子元素可以有一个id(必填),name(可选) -->
<!-- 
	启动一个流程可以通过调用 executionService.startProcessInstanceByKey("myBusinessProcess")。如果没有指定 name，id会被用作 key。
	这个启动方法和 startProcessInstanceById不同。ById方法期望使用 Activiti引擎在发布时自动生成的id，可以通过processDefinition.getId()
	方法获得这个值。生成的id格式为 'key:version'，最大长度限制为64个字符，如果你在启动时抛出了一个ActivitiException，说明生成的id太长了，需要限制
	流程key的长度。
	基本上 name和 key在使用上是等价的。key的规则与jPDL一样，空格和非字母数字的字符会被下划线替代。
 -->
  <process id="My business processs" name="myBusinessProcess">

      

  </process>
<definitions>